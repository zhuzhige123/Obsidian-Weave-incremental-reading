/**
 * Default ESLint flat config (discovered by Obsidian community scorecard).
 * Mirrors eslint.community.config.mjs so local + review scanners share one profile.
 */
export { default } from "./eslint.community.config.mjs";
